# Project repository
https://github.com/Vayala13/LLM-COST-Autopilot.git

# Writing standard
This file uses ASD-STE100 Simplified Technical English (controlled style).
Technical names (file paths, model IDs, commands) stay unchanged.

## Timeline

- 2026-07-23 — Phase 1.1–1.3. The team made `app/providers/` with ModelConfig, Response, and `send_request`. The team ran 10 baseline prompts with `scripts/baseline_test.py`. The Anthropic smoke test passed. OpenAI and Ollama did not run. The team made `.env`, `config.py`, a venv, and a portfolio site.
- 2026-07-24 — Phase 1.3. The team installed Ollama again. The empty app bundle was the cause of the stop. The team pulled `llama3.2` and started the daemon. The team ran the 10-prompt baseline on `claude-sonnet`, `claude-haiku`, and `llama-local`. The log has 30 records in `data/baseline_results.json`. Cost totals: Sonnet $0.0386, Haiku $0.0061 (84% less), Llama $0.00. The team added the Gemini provider. `gemini-flash` works as a Tier 2 model. The team removed `gemini-pro` because of a 429 quota. The team installed a Figma plugin and made an architecture diagram.
- 2026-07-24 — Phase 2.1. The team set 3 complexity tiers in `configs/complexity_tiers.yaml`. The active provider set is `claude-sonnet`, `claude-haiku`, `gemini-flash`, and `llama-local`. The team stopped the bad OpenAI key in `.env`. Baseline runs skip OpenAI.
- 2026-07-29 — Phase 2.2. The team made `data/labeled_prompts.jsonl` with 201 labeled prompts (72 / 62 / 67 for tiers 1 / 2 / 3). The team made `app/classifier/features.py` to get 11 features per prompt. `scripts/inspect_dataset.py` checks balance and writes `data/prompt_features.json`. The team used coding-guidelines and security-best-practices. PR #1 had a review and a squash merge to `main`.
- 2026-07-31 — Phase 2.3. The team trained logistic regression and random forest on 11 features and 201 prompts. Each model had 88.2% held-out accuracy. The selected model is logistic regression in `models/complexity_classifier.joblib`. PR #2 had a review and a merge.
- 2026-07-31 — Phase 2.4. The team added `configs/routing_map.yaml`. Tier 1 maps to `llama-local`. Tier 2 maps to `gemini-flash`. Tier 3 maps to `claude-sonnet`. The team made `app/router/map.py` with `load_routing_map`, `model_for_tier`, and `route_prompt`. The loader checks registry keys. The smoke script is `scripts/show_routing.py`. The team used security-best-practices and coding-guidelines. The PR had a full review before merge. Phase 2 is complete.
- 2026-08-01 — Phase 3.1. The team added `configs/quality_thresholds.yaml`. Extraction uses `field_coverage >= 1.0`. Summarization uses `llm_judge_score > 4/5` with judge `claude-sonnet`. Classification uses `label_agreement == 1.0` with reference `claude-sonnet`. GPT-4o is not available while OpenAI is off. The loader is `app/quality/thresholds.py`. The smoke script is `scripts/show_quality_thresholds.py`. Cloud agent PR #4 had guidelines, security checks, and a review. The PR merged to `main`.
- 2026-08-01 — Phase 3.2. The team made the async quality verifier in `app/quality/verifier.py`. Metrics use YAML thresholds and `send_request`. The queue is `app/quality/queue.py` (`enqueue` / `drain`). Routing failures go to `data/routing_failures.jsonl` with a prompt hash. Bugbot fixes: whole-word field match, last in-range judge score, and label punctuation strip. Cloud agent PR #5 had a squash merge to `main`.
- 2026-08-02 — Phase 3.3. The team made auto-escalation on routing failure. Config is `configs/escalation.yaml` (`escalation_model` = `claude-sonnet`, `max_escalation_latency_s` = 3.0). Code is `app/quality/escalation.py` (`escalate_if_needed`). Logs go to `data/escalations.jsonl` with a prompt hash. The smoke script is `scripts/smoke_escalation.py`. Cloud agent PR #7 was ready for review and had a squash merge to `main`.
- 2026-08-02 — Phase 3.4. The team made classifier feedback. `app/classifier/feedback.py` records labeled examples on routing failure. The new tier is the routed tier plus 1, with a maximum of 3. Examples go to `data/feedback_prompts.jsonl` (gitignored). `scripts/retrain_from_feedback.py` merges base labels and feedback, then trains again. The hook is in `verify()`. The smoke script is `scripts/smoke_feedback.py`. Cloud agent PR #8 was ready for review and had a squash merge to `main`. Phase 3 is complete.
- 2026-08-02 — Phase 4.1. The team made the SQLite audit store in `app/audit/store.py` (`init_db`, `log_request`, `log_completion`, `fetch_requests`). Each completion has one row: timestamp, prompt_hash, complexity_tier, routed_model, cost, latency, verifier_quality_score, escalated. Optional fields: escalation_model, cost_delta, use_case. Writes use parameterized SQL only. The database file `data/requests.db` is gitignored. The smoke script is `scripts/smoke_audit.py` (offline). Cloud agent PR #9 was ready for review and had a squash merge to `main`.
- 2026-08-02 — Phase 4.2. The team made the cost dashboard. Metrics are in `app/metrics/cost.py` (actual cost vs GPT-4o, savings, routing share, quality scores, escalation rate). The UI is `dashboard/app.py` on localhost. An empty database can load demo data (about 37% savings). Audit rows can store `input_tokens` and `output_tokens`. If tokens are missing, the fallback is 500 input / 250 output. The smoke script is `scripts/smoke_metrics.py`. The team pinned streamlit, pandas, and altair. Cloud agent PR #10 had a squash merge to `main`.
- 2026-08-02 — Phase 4.3. The team set the primary cost metric `DashboardSummary.cost_reduction_pct` (vs all GPT-4o). The Streamlit UI shows the large percent and dollars saved. The CLI is `python -m scripts.show_savings --demo` (about 36.7% on the seed). README and the portfolio site refer to this metric. Cloud agent PR #11 had a squash merge to `main`. Phase 4 is complete.
- 2026-08-02 — Phase 5.1. The team made FastAPI `POST /v1/completions` in `app/api/`. The client cannot set the model (`extra=forbid`). Flow: `route_prompt` → `send_request` → `log_completion` → optional async verify. The smoke script is `scripts/smoke_api.py`. The team pinned fastapi, uvicorn, and httpx. Run: `uvicorn app.api.main:app --host 127.0.0.1 --port 8000`. Cloud agent PR #12 had a squash merge to `main`.
- 2026-08-02 — Phase 5.2. The team made config endpoints in `app/api/config_routes.py`: `GET /v1/models`, `GET /v1/stats`, `GET|PUT /v1/routing-config`. Stats use aggregate data only. PUT checks MODEL_REGISTRY keys and writes only under `configs/`. The write gate is `ALLOW_ROUTING_CONFIG_WRITE`. Smoke tests are in `scripts/smoke_api.py`. Cloud agent PR #13 had a squash merge to `main`.
- 2026-08-02 — Phase 5.3. The team made `Dockerfile` and `docker-compose.yml` (api + worker, shared `./data`). The API runs verify in the same process. The worker watches `data/` and can retrain. The team added `.env.example`. README shows savings and the architecture diagram. The smoke script is `scripts/smoke_worker.py`. Cloud agent PR #14 had a squash merge to `main`. Phase 5 is complete.
- 2026-08-02 — Phase 6.1. The team made an offline load test in `scripts/load_test.py` (n=750). The classifier and routing map are real. Provider costs come from the registry mock. Reports: `reports/load_test_savings.json`, `reports/load_test_savings.md`, `reports/load_test_dashboard.png`. Headline: 30.6% less cost vs all GPT-4o (saved $0.3656). Cloud agent PR #15 had a squash merge to `main`.
- 2026-08-02 — Phase 6.2. The team made `CASE_STUDY.md`. The lead number is 30.6%. The text covers routing, verify, escalate, and feedback. The escalation rate in the offline test is 3.2%. Work ran locally (no cloud agent). The build plan is complete.

## Current status (as of 2026-08-02)

- Phase 6 of 6 (Portfolio polish) is complete.
- All six phases are complete.
- Headline: 30.6% cost reduction vs all GPT-4o (n=750).
- Optional next work: live tests with API keys, API auth, and portfolio OWNER fields.
